import React, {Component} from 'react'
import Button from './Button.js'

export default class extends Component {
  render() {
    return <div>
      <Button>
        Button
      </Button>
    </div>
  }
}
