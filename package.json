{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "yarn radix-system:build && yarn radix:build && yarn icons:build",
    "start": "npm-run-all --parallel radix website",
    "website": "lerna exec --scope website -- yarn start",
    "website:build": "lerna exec --scope website -- yarn build",
    "radix": "lerna exec --scope @modulz/radix -- yarn watch",
    "radix:build": "lerna exec --scope @modulz/radix -- yarn build",
    "radix-system": "lerna exec --scope @modulz/radix-system -- yarn watch",
    "radix-system:build": "lerna exec --scope @modulz/radix-system -- yarn build",
    "icons": "lerna exec --scope @modulz/radix-icons -- yarn watch",
    "icons:build": "lerna exec --scope @modulz/radix-icons -- yarn build",
    "build": "yarn radix-system:build && yarn radix:build && yarn icons:build && yarn website:build",
    "prettier": "prettier './**/*.{js,jsx,ts,tsx,json,yml,yaml,css,md}' --write",
    "lint": "eslint './packages/**/*.{js,jsx,ts,tsx}'",
    "clean": "npm run clean-logs && npm run clean-modules",
    "clean-logs": "rimraf ./packages/*/npm-debug* && rimraf ./*-debug*",
    "clean-modules": "rimraf ./packages/*/node_modules/ && rimraf ./node_modules/",
    "release": "yarn radix-system:build && yarn radix:build && lerna publish"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^1.11.0",
    "@typescript-eslint/parser": "^1.11.0",
    "babel-eslint": "10.0.2",
    "eslint": "5.16.0",
    "eslint-config-react-app": "^4.0.1",
    "eslint-plugin-flowtype": "3.11.1",
    "eslint-plugin-import": "2.18.0",
    "eslint-plugin-jsx-a11y": "6.2.3",
    "eslint-plugin-react": "7.14.2",
    "eslint-plugin-react-hooks": "1.6.1",
    "husky": "^2.7.0",
    "lerna": "^3.15.0",
    "lint-staged": "^9.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "yarnhook": "^0.4.1"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "lint-staged",
      "post-merge": "yarnhook",
      "post-checkout": "yarnhook",
      "post-rewrite": "yarnhook"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix",
      "git add"
    ],
    "*.{md,json}": [
      "prettier --write",
      "git add"
    ]
  }
}
