---
title: Card Link
component: CardLink
---

```js live=true
<CardLink maxWidth={275}>
  <Heading size={1} mb="3" bold>
    Design tools are running out of track. Here’s how we can fix them.
  </Heading>
  <Text as="p" mb="4" fontSize="3">
    An outline of two opposing narratives emerging in the design tool space.
  </Text>
  <Text fontSize="2" color="grays.5">
    Today
  </Text>
</CardLink>
```

```js live=true
<CardLink maxWidth={275} variant="shadow">
  <Heading size={1} mb="3" bold>
    Design tools are running out of track. Here’s how we can fix them.
  </Heading>
  <Text as="p" mb="4" fontSize="3">
    An outline of two opposing narratives emerging in the design tool space.
  </Text>
  <Text fontSize="2" color="grays.5">
    Today
  </Text>
</CardLink>
```

```js live=true
<CardLink maxWidth={275} variant="ghost">
  <Heading size={1} mb="3" bold>
    Design tools are running out of track. Here’s how we can fix them.
  </Heading>
  <Text as="p" mb="4" fontSize="3">
    An outline of two opposing narratives emerging in the design tool space.
  </Text>
  <Text fontSize="2" color="grays.5">
    Today
  </Text>
</CardLink>
```

The `CardLink` component composes the `Card` [component](/docs/card)

### System props

<SystemProps props={['color', 'space', 'width', 'maxWidth']} />

### Custom props

| Name      | Type                |   Default   | Description        |
| :-------- | :------------------ | :---------: | :----------------- |
| `variant` | 'shadow' \| 'ghost' | `undefined` | Variant to display |
