---
title: Tooltip
component: Tooltip
description: An accessible Tooltip component that shows information related to an element when it is hovered or focused.
---

```js live
<Tooltip label="Hey there, now try focusing" side="bottom" align="center">
  <Button size={1}>Hover me</Button>
</Tooltip>
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render as target',
    },
    label: {
      type: 'String',
      description: 'The content to render in the Tooltip',
    },
    ariaLabel: {
      type: 'String',
      description: 'The content to render in the arial-label attributex',
    },
    side: {
      type: 'top | bottom | right | left',
      default: 'bottom',
      description: 'The side to be positioned',
    },
    sideOffset: {
      type: 'Number',
      default: '0',
      description: 'The side offset to apply',
    },
    align: {
      type: 'start | center | end',
      default: 'center',
      description: 'The alignment to be positioned',
    },
    alignOffset: {
      type: 'Number',
      default: '0',
      description: 'The align offset to apply',
    },
    arrowOffset: {
      type: 'Number',
      default: '6',
      description: 'The arrow offset to apply',
    },
    noArrow: {
      type: 'Boolean',
      default: 'false',
      description: 'Whether the arrow should be visible or not',
    },
    collisionTolerance: {
      type: 'Number',
      default: '0',
      description: 'The collision tolerance to apply',
    },
  }}
/>

## Examples

### Side

There are 4 side options to choose from.

```js live
<Tooltip label="Tooltip" side="top">
  <Button>Top</Button>
</Tooltip>

<Tooltip label="Tooltip" side="bottom">
  <Button ml={6}>Bottom</Button>
</Tooltip>

<Tooltip label="Tooltip" side="right">
  <Button ml={6}>Right</Button>
</Tooltip>

<Tooltip label="Tooltip" side="left">
  <Button ml={6}>Left</Button>
</Tooltip>
```

### Side offset

Control the side offset distance.

```js live
<Tooltip label="Tooltip" side="top" sideOffset={6}>
  <Button>Side offset</Button>
</Tooltip>
```

### Align

There are 3 alignment options to choose from.

```js live
<Tooltip label="Tooltip" align="start">
  <Button>Start</Button>
</Tooltip>

<Tooltip label="Tooltip" align="center">
  <Button mx={6}>Center</Button>
</Tooltip>

<Tooltip label="Tooltip" align="end">
  <Button>end</Button>
</Tooltip>
```

### Align offset

Control the alignment offset distance.

```js live
<Tooltip label="Tooltip" alignOffset={10} align="start">
  <Button>Align offset</Button>
</Tooltip>
```

### Arrow offset

Control the arrow offset distance.

```js live
<Tooltip label="Tooltip" arrowOffset={3}>
  <Button>Arrow offset</Button>
</Tooltip>
```
