---
title: Select
component: Select
description: Useful for allowing users to select only one option from a set.
---

```js live
<Select>
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
</Select>
```

<PropsTable
  data={{
    children: {
      type: 'Option | OptionGroup',
      description: 'The options or option group to render',
    },
    onValueChange: {
      type: 'Function (value: string)',
      description: 'A function to be called when the value changes',
    },
    size: {
      type: '0 | 1',
      default: '0',
      description: 'The size to apply',
    },
    variant: {
      type: 'normal | ghost',
      default: 'normal',
      description: 'The variant to apply',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  Option
</Heading>


<PropsTable
  data={{
    value: {
      type: 'String',
      description: 'The value to apply',
    },
    label: {
      type: 'String',
      description: 'The label to display',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  OptionGroup
</Heading>


<PropsTable
  data={{
    children: {
      type: 'Option',
      description: 'The options to render',
    },
    label: {
      type: 'String',
      description: 'The group label to apply',
    },
    disabled: {
      type: 'Boolean',
      description: 'Whether it is disabled or not',
    },
  }}
/>

## Examples

### Variant

There are 2 variants options to choose from.

```js live
<Select variant="normal">
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
</Select>

<Select variant="ghost" ml={6}>
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
</Select>
```

### Sizes

There are 2 sizes to choose from.

```js live
<Select size={0}>
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
</Select>

<Select size={1} ml={6}>
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
</Select>
```

### With groups

Group options using the `OptionGroup` Component

```js live
<Select size={0}>
  <Option value="option1" label="Option one" />
  <Option value="option2" label="Option two" />
  <Option value="option3" label="Option three" />
  <OptionGroup label="Option group">
    <Option value="option-group1" label="Option group 1" />
    <Option value="option-group2" label="Option group 2" />
    <Option value="option-group3" label="Option group 3" />
  </OptionGroup>
</Select>
```
