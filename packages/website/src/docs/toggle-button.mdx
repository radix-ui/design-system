---
title: Toggle Button
component: ToggleButton
description: Useful for allowing users to toggle a single selection.
---

```js live removeFragment
() => {
  const [shouldNotify, setShouldNotify] = React.useState(false);

  return (
    <ToggleButton
      isToggled={shouldNotify}
      onToggle={setShouldNotify}
      sx={{ width: 100 }}
    >
      Notify me
    </ToggleButton>
  );
};
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render',
    },
    variant: {
      type: 'normal | fade',
      default: 'normal',
      description: 'The variant to apply',
    },
    size: {
      type: '0 | 1',
      default: '1',
      description: 'The size to apply',
    },
  }}
/>
