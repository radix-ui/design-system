---
title: Card
component: Card
---

```js live=true
<Card maxWidth={275}>
  <Text as="p" mb="5" size={3}>
    Illustration tools can't be the source of truth for code. It might demo well with simple
    examples, but long term it's a dead end.
  </Text>
  <Text size={2} textColor="grays.5">
    <Link>Mark Dalgleish</Link>
  </Text>
</Card>
```

```js live=true
<Card maxWidth={275} variant="shadow">
  <Text as="p" mb="5" size={3}>
    Illustration tools can't be the source of truth for code. It might demo well with simple
    examples, but long term it's a dead end.
  </Text>
  <Text size={2} textColor="grays.5">
    <Link>Mark Dalgleish</Link>
  </Text>
</Card>
```

```js live=true
<Card maxWidth={275} variant="ghost">
  <Text as="p" mb="5" size={3}>
    Illustration tools can't be the source of truth for code. It might demo well with simple
    examples, but long term it's a dead end.
  </Text>
  <Text size={2} textColor="grays.5">
    <Link>Mark Dalgleish</Link>
  </Text>
</Card>
```

### System props

<SystemProps props={['textColor', 'margin', 'padding', 'width', 'maxWidth']} />

### Custom props

| Name      | Type                            | Default  | Description        |
| :-------- | :------------------------------ | :------: | :----------------- |
| `variant` | 'shadow' \| 'shadow' \| 'ghost' | `border` | Variant to display |
