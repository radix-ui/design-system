---
title: Card
component: Card
description: Useful to group information and actions about a single subject.
---

```js live
<Card maxWidth={275}>
  <Heading size={1} fontWeight={500}>
    Card heading
  </Heading>
  <Text as="p" mt={2} mb={4} size={3}>
    Card copy
  </Text>
  <Button>Action</Button>
</Card>
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render',
    },
    variant: {
      type: 'normal | shadow | ghost',
      default: 'normal',
      description: 'The variant to apply',
    },
  }}
/>

<SystemProps
  props={[
    'textColor',
    'backgroundColor',
    'margin',
    'padding',
    'width',
    'maxWidth',
  ]}
/>

## Examples

### Variants

There are 3 variants to choose from.

```js live
<Card maxWidth={275} variant="shadow">
  <Heading size={1} fontWeight={500}>
    Shadow card
  </Heading>
  <Text as="p" mt={2} mb={4} size={3}>
    Card copy
  </Text>
  <Button>Action</Button>
</Card>
```

```js live
<Card maxWidth={275} variant="ghost">
  <Heading size={1} fontWeight={500}>
    Ghost card
  </Heading>
  <Text as="p" mt={2} mb={4} size={3}>
    Card copy
  </Text>
  <Button>Action</Button>
</Card>
```
