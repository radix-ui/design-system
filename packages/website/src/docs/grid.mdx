---
title: Grid
component: Grid
description: A primitive useful for grid layouts.
---

```js live
<Grid sx={{ gridTemplateColumns: 'repeat(5, 1fr)', gap: 6 }}>
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
  <AspectRatio sx={{ bg: 'blue600' }} />
</Grid>
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render',
    },
  }}
/>
