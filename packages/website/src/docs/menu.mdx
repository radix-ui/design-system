---
title: Menu
component: Menu
description: Useful for displaying a list of actions. Typically not used directly but in conjunction with other components like Right Click Menu or Dropdown Menu.
disableLock: true
---

```js live removeFragment
() => {
  const [isChecked, setIsChecked] = React.useState(true);
  const [value, setValue] = React.useState('2');
  const targetRef = React.useRef();

  return (
    <Box sx={{ height: '243px' }}>
      <div ref={targetRef} />
      <Menu isOpen buttonRef={targetRef}>
        <MenuItem label="Simple item one" />
        <MenuItem label="Simple item two" />
        <MenuItem label="Simple item three" />
        <MenuItemSeparator />
        <MenuItemLabel>An item label</MenuItemLabel>
        <MenuItemSeparator />
        <MenuItemCheckbox
          label="Checkbox item"
          checked={isChecked}
          onChange={setIsChecked}
        />
        <MenuItemSeparator />
        <MenuItemRadioGroup value={value} onChange={setValue}>
          <MenuItemRadio value="1" label="Radio item one" />
          <MenuItemRadio value="2" label="Radio item two" />
          <MenuItemRadio value="3" label="Radio item three" />
        </MenuItemRadioGroup>
      </Menu>
    </Box>
  );
};
```

<PropsTable
  data={{
    children: {
      type:
        'MenuItem | MenuItemLabel | MenuItemSeparator | MenuItemCheckbox | MenuItemRadioGroup',
      description: 'The items to render',
    },
    buttonRef: {
      type: 'React.RefObject',
      description:
        'A reference to a button element used to position the menu below',
    },
    buttonTextRef: {
      type: 'React.RefObject',
      description:
        'A reference to the text element inside of a button, used as extra positioning information when the menu is used to build a Select',
    },
    anchorPoint: {
      type: 'Point (x, y)',
      description:
        'An alternative way to position the menu directly by referencing a point in space, used when opening a menu via right click',
    },
    isOpen: {
      type: 'Boolean',
      default: 'false',
      description: 'Whether it is open or not',
    },
    onClose: {
      type: 'Function',
      description: 'A function to be called when it closes',
    },
    highlightedIndex: {
      type: 'Number',
      description: 'The index of the highlighted item (if controlled)',
    },
    onHighlightedIndexChange: {
      type: 'Function (index?: number) => void',
      description: 'A function called when the highlighted item changes',
    },
    defaultHighlightedIndex: {
      type: 'Number',
      description:
        'The index of the item to highlithed by default (if uncontrolled)',
    },
    onItemSelected: {
      type: 'Function (item: Item) => void',
      description: 'A callback function called when an item has been selected',
    },
    calculateMenuPosition: {
      type:
        'Function (options: CalculateMenuPositionOptions) => MenuPosition | undefined',
      description:
        'A function used to specify how to calculate the menu position',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItem
</Heading>


<PropsTable
  data={{
    label: {
      type: 'String',
      description: 'The label to display',
    },
    value: {
      type: 'String',
      description: 'An optional value for the item',
    },
    disabled: {
      type: 'Boolean',
      description: 'Whether it is disabled or not',
    },
    onSelect: {
      type: 'Function',
      description: 'A function called when the item is selected',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItemSeparator
</Heading>


<PropsTable data={{}} />


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItemLabel
</Heading>


<PropsTable
  data={{
    children: {
      type: 'String',
      description: 'The label to display',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItemCheckbox
</Heading>


<PropsTable
  data={{
    label: {
      type: 'String',
      description: 'The label to display',
    },
    value: {
      type: 'String',
      description: 'An optional value for the item',
    },
    disabled: {
      type: 'Boolean',
      description: 'Whether it is disabled or not',
    },
    checked: {
      type: 'Boolean',
      description: 'Whether it is checked or not (if controlled)',
    },
    onChange: {
      type: 'Function (checked?: boolean) => void',
      description: 'A function called when it is checked/unchecked',
    },
    defaultChecked: {
      type: 'Boolean',
      description: 'Whether it is initially checked or not (if uncontrolled)',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItemRadioGroup
</Heading>


<PropsTable
  data={{
    children: {
      type: 'MenuItemRadio',
      description: 'The radio items to render',
    },
    value: {
      type: 'String',
      description: 'The selected value (if controlled)',
    },
    onChange: {
      type: 'Function (value?: string) => void',
      description: 'A function called when the selected value changes',
    },
    defaultValue: {
      type: 'String',
      description: 'The default selected value (if uncontrolled)',
    },
  }}
/>


<Heading as="h2" size={4} mb={2} mt={6} sx={{ lineHeight: 4 }}>
  MenuItemRadio
</Heading>


<PropsTable
  data={{
    label: {
      type: 'String',
      description: 'The label to display',
    },
    value: {
      type: 'String',
      description: 'A value for the item',
    },
    disabled: {
      type: 'Boolean',
      description: 'Whether it is disabled or not',
    },
  }}
/>
