import { system, Config } from 'styled-system';
export { JustifyContentProps } from 'styled-system';

export const config: Config = { justifyContent: true };

export const justifyContent = system(config);
