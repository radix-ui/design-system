import { system, Config } from '@styled-system/core';
export { JustifyItemsProps } from '@styled-system/core';

export const config: Config = { justifyItems: true };

export const justifyItems = system(config);
