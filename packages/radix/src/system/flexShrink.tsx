import { system, CustomStyleDefinitions } from 'styled-system';
export { FlexShrinkProps } from 'styled-system';

const config: CustomStyleDefinitions = { flexShrink: true };

export const flexShrink = system(config);
