import { system, CustomStyleDefinitions } from 'styled-system';
export { LineHeightProps } from 'styled-system';

const config: CustomStyleDefinitions = { lineHeight: true };

export const lineHeight = system(config);
