import { system, CustomStyleDefinitions } from 'styled-system';
export { JustifySelfProps } from 'styled-system';

const config: CustomStyleDefinitions = { justifySelf: true };

export const justifySelf = system(config);
