import { system, CustomStyleDefinitions } from 'styled-system';
export { AlignSelfProps } from 'styled-system';

const config: CustomStyleDefinitions = { alignSelf: true };

export const alignSelf = system(config);
