import { system, CustomStyleDefinitions } from 'styled-system';
export { OverflowProps } from 'styled-system';

const config: CustomStyleDefinitions = { overflow: true };

export const overflow = system(config);
