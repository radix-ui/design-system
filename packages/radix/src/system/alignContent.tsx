import { system, CustomStyleDefinitions } from 'styled-system';
export { AlignContentProps } from 'styled-system';

const config: CustomStyleDefinitions = { alignContent: true };

export const alignContent = system(config);
