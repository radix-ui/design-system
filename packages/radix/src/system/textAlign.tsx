import { system, CustomStyleDefinitions } from 'styled-system';
export { TextAlignProps } from 'styled-system';

const config: CustomStyleDefinitions = { textAlign: true };

export const textAlign = system(config);
