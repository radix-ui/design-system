import { system, CustomStyleDefinitions } from 'styled-system';
export { FlexGrowProps } from 'styled-system';

const config: CustomStyleDefinitions = { flexGrow: true };

export const flexGrow = system(config);
