import { system, CustomStyleDefinitions } from 'styled-system';
export { GridAutoFlowProps } from 'styled-system';

export const config: CustomStyleDefinitions = { gridAutoFlow: true };

export const gridAutoFlow = system(config);
