import { system, CustomStyleDefinitions } from 'styled-system';
export { GridTemplateRowsProps } from 'styled-system';

export const config: CustomStyleDefinitions = { gridTemplateRows: true };

export const gridTemplateRows = system(config);
