import { system, CustomStyleDefinitions } from 'styled-system';
export { GridRowProps } from 'styled-system';

export const config: CustomStyleDefinitions = { gridRow: true };

export const gridRow = system(config);
