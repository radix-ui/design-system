import { system, CustomStyleDefinitions } from 'styled-system';
export { JustifyContentProps } from 'styled-system';

export const config: CustomStyleDefinitions = { justifyContent: true };

export const justifyContent = system(config);
