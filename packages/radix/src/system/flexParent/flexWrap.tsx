import { system, CustomStyleDefinitions } from 'styled-system';
export { FlexWrapProps } from 'styled-system';

export const config: CustomStyleDefinitions = { flexWrap: true };

export const flexWrap = system(config);
