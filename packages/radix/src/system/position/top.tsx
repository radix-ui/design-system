import { system, CustomStyleDefinitions } from 'styled-system';
export { TopProps } from 'styled-system';

export const config: CustomStyleDefinitions = {
  top: true,
};

export const top = system(config);
